{
  "schema_version": "1.0.0",
  "category": "encryption",
  "normalized_category": "Encryption",
  "default": {
    "finding_type": "Security",
    "severity": "HIGH"
  },
  "rules": {
    "s3_bucket_encryption": {
      "severity": "HIGH",
      "why_it_matters": "A bucket without server-side encryption stores objects unencrypted at rest, so the data is readable to anyone who obtains access to the underlying storage or to a copy of the objects.",
      "recommendation": "Add a BucketEncryption property with a ServerSideEncryptionConfiguration entry, using SSEAlgorithm AES256 or aws:kms."
    },
    "rds_storage_encrypted": {
      "severity": "HIGH",
      "why_it_matters": "Storage encryption on an RDS DB instance cannot be enabled in place after creation, so an unencrypted instance keeps its data, snapshots and read replicas unencrypted for the whole life of the resource.",
      "recommendation": "Set StorageEncrypted to true, and set KmsKeyId when a customer managed key is required."
    },
    "ebs_volume_encrypted": {
      "severity": "HIGH",
      "why_it_matters": "An unencrypted EBS volume exposes data at rest to anyone who gains access to the underlying storage or a snapshot.",
      "recommendation": "Set Encrypted to true on each Ebs block device mapping, or use an encrypted AMI."
    },
    "sns_topic_encrypted": {
      "severity": "MEDIUM",
      "why_it_matters": "Messages in an unencrypted SNS topic can be read by anyone with access to the underlying storage infrastructure.",
      "recommendation": "Set KmsMasterKeyId to a KMS key ARN or alias."
    },
    "logs_group_encrypted": {
      "severity": "MEDIUM",
      "why_it_matters": "CloudWatch Logs data without customer-managed encryption relies solely on service-managed encryption, which may not satisfy compliance requirements.",
      "recommendation": "Set KmsKeyId on the log group to a customer-managed KMS key."
    },
    "sqs_queue_encrypted": {
      "severity": "MEDIUM",
      "why_it_matters": "Messages in an unencrypted SQS queue can be read by anyone with access to the underlying storage.",
      "recommendation": "Set KmsMasterKeyId to a KMS key ARN or alias."
    },
    "dynamodb_encryption": {
      "severity": "MEDIUM",
      "why_it_matters": "Without customer-managed encryption, DynamoDB data relies solely on AWS-owned keys and cannot satisfy compliance requirements that mandate key control.",
      "recommendation": "Add SSESpecification with SSEEnabled: true and KMSMasterKeyId."
    },
    "kinesis_encryption": {
      "severity": "HIGH",
      "why_it_matters": "Unencrypted Kinesis streams expose streaming data to anyone with underlying storage access.",
      "recommendation": "Add StreamEncryption with EncryptionType: KMS and a KeyId."
    },
    "redshift_encryption": {
      "severity": "HIGH",
      "why_it_matters": "An unencrypted Redshift cluster exposes warehouse data at rest.",
      "recommendation": "Set Encrypted to true and specify a KmsKeyId."
    },
    "elasticache_encryption": {
      "severity": "HIGH",
      "why_it_matters": "Without encryption, cached data is exposed in transit and at rest, including session tokens and credentials.",
      "recommendation": "Set TransitEncryptionEnabled and AtRestEncryptionEnabled to true, and configure an AuthToken."
    },
    "efs_encryption": {
      "severity": "HIGH",
      "why_it_matters": "An unencrypted EFS file system exposes file data to anyone with underlying storage access.",
      "recommendation": "Set Encrypted to true and optionally provide a KmsKeyId."
    }
  }
}
